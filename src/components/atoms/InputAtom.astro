---
import type { HTMLAttributes } from "astro/types";
type Props = HTMLAttributes<"input">;
const { type } = Astro.props as Props;
---

<input
  type={type}
  class="sm:px-4 sm:py-2 sm:rounded-[.4em] w-full outline-none focus:outline-2 focus:ring-2 focus:ring-[#C49381] bg-white rounded-[.3em] py-1 px-2"
/>
