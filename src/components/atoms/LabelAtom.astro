---
import type { HTMLAttributes } from "astro/types";
type Props = HTMLAttributes<"label">;
const { title, ...atrs } = Astro.props as Props;
---

<label {...atrs} class="xl:text-lg text-sm text-secondary font-[OpenSansLight]"
  >{title}</label
>
