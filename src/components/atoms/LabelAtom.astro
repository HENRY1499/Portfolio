---
import type { HTMLAttributes } from "astro/types";
type Props = HTMLAttributes<"label">;
const { title, ...atrs } = Astro.props as Props;
---

<label
  {...atrs}
  class="xl:text-lg sm:text-[16px] text-sm text-secondary font-[OpenSansLight]"
  >{title}</label
>
