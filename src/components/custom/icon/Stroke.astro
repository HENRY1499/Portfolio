---
export interface Props {
  className?: string;
  width?: number; // Ej: '24px', '2rem', etc.
  height?: number; // Ej: '24px', '2rem', etc.
  color?: string; // Ej: '#000', 'currentColor', etc.
}

const { className = "", width = 631, height = 2 } = Astro.props;
---

<svg
  class={className}
  width={`${width}px`}
  height={`${height}px`}
  viewBox="0 0 631 2"
  fill="none"
  xmlns="http://www.w3.org/2000/svg"
>
  <path id="path" d="M0 1H631" stroke="#9B7D61" stroke-width="6"></path>
</svg>
